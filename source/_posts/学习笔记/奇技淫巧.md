## 1.基于内存的动态分页

```java
        sysInformetions.stream().skip((approvalVO.getCurrent()-1)*approvalVO.getSize()).limit(approvalVO.getSize()).
                collect(Collectors.toList());
        int pages = sysInformetions.size()/approvalVO.getSize();
        if (sysInformetions.size()%approvalVO.getSize()>0){
            pages ++;
        }
        Map<String,Object> returnMap = new HashMap<>();
        returnMap.put("records",sysInformetions);
        returnMap.put("current",approvalVO.getCurrent());
        returnMap.put("size",approvalVO.getApproval());
        returnMap.put("total",sysInformetions.size());
        returnMap.put("pages",pages)
```

